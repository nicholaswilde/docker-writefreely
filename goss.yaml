---
port:
  tcp6:8080:
    listening: true

process:
  writefreely:
    running: true

http:
  http://localhost:8080:
    status: 200
    allow-insecure: false
    no-follow-redirects: false
    timeout: 0
    body:
      - <title>blog</title>
